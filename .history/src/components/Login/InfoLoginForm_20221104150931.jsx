import React from 'react'
import { useForm } from 'react-hook-form'



const InfoLoginForm = () => {

    const {
        register,
        handleSubmit,
        formState: { errors }
     } = useForm()

  return (
    <form>
      
    </form>
  )
}

export default InfoLoginForm
