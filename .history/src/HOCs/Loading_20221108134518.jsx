import React from 'react'

export const Loading = (component) => {
    const newComponent = ({...props}) => {
        return (
            <>
            </>
        )
    }
    return new
}

export default Loading
