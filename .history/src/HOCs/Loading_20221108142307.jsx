import React from 'react'
import from 'react-spin'

export const Loading = (component) => {
    const newComponent = ({...props}) => {
        return (
            <>

            </>
        )
    }
    return newComponent
}

