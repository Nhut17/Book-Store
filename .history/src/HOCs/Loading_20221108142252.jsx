import React from 'react'
import from 

export const Loading = (component) => {
    const newComponent = ({...props}) => {
        return (
            <>

            </>
        )
    }
    return newComponent
}

