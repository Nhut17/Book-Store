import React from 'react'
import from 'reac'

export const Loading = (component) => {
    const newComponent = ({...props}) => {
        return (
            <>

            </>
        )
    }
    return newComponent
}

