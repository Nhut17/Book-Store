import React from 'react'
import from 'rea'

export const Loading = (component) => {
    const newComponent = ({...props}) => {
        return (
            <>

            </>
        )
    }
    return newComponent
}

