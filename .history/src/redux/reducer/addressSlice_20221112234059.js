import { createSlice } from "@reduxjs/toolkit";


const initialState = {

}

const getAllAddresses = create

const addressSlice = createSlice({
    name: 'address',
    initialState,
    extraReducers: {

    }
})