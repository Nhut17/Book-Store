import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    listCart: [],
    success:
}

const categorySlice = createSlice({
    name: 'category',
    initialState,
    extraReducers: {

    }
})