import {createSlice} from '@reduxjs/toolkit'

const initialState = {
    user: [],
    loading: true,
}


export const loginUser = createSy

const userReducer = createSlice({
    name: 'user',
    initialState,
    extraReducers: {

    }
})


export default userReducer.reducer